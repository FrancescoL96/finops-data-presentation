# The values that go here will be the ones to compile the CRD generated by KOG

filter: ""

scraperConfig:
  tableName: pricing_table
  pollingIntervalHours: 1
  scraperDatabaseConfigRef: 
    name: cratedb-config
    namespace: krateo-system