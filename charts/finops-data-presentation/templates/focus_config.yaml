apiVersion: finops.krateo.io/v1
kind: FocusConfig
metadata:
  name: {{ include "finops-data-presentation.fullname" . }}
spec:
  focusSpec: 
    listUnitPrice: {{ (lookup "finops.krateo.io/v1alpha1" "DataPresentationTest" .Release.Namespace (include "finops-data-presentation.fullname" .) ).status.rating | default 0 }}
    pricingUnit: "Hours"